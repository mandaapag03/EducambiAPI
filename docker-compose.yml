volumes:
  data:
services:
  database:
    container_name: educambi_db_mysql
    image: mysql:9.1
    build: mysql:9.1
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_USER: educambi_manager_user
      MYSQL_PASSWORD: ASDQWEZXC#147
      MYSQL_DATABASE: db_educambi
    ports:
      - "3306:3306"
    networks:
      - educambi
    volumes:
      - data:/var/lib/mysql
      - ./config/docker-entrypoint-initdb.d/init.sql:/docker-entrypoint-initdb.d/init.sql

  go-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    container_name: go-app-container

networks:
  educambi:
    driver: bridge